{
    "layer": "top",
    "position": "top",
    "height": 30,
    "spacing": 4,
    "modules-left": [
        "group/audio-brightness",
        "group/network-bluetooth"
    ],

    "modules-center": [
        "clock"
    ],

    "modules-right": [
        "cpu",
        "memory",
        "disk",
        "battery"
    ],

    "group/audio-brightness": {
        "orientation": "vertical",
        "modules": [
            "pulseaudio",
            "backlight"
        ]
    },

    "pulseaudio": {
        "format": "AUDIO: {volume} percent",
        "format-muted": "AUDIO: MUTED",
        "on-click": "pavucontrol"
    },


    "group/network-bluetooth": {
        "orientation": "vertical",
        "modules": [
            "network",
            "bluetooth"
        ]
    },

    "network": {
        "format-wifi": "NETWORK: {essid} {signalStrength} percent",
        "format-ethernet": "NETWORK: Ethernet {ifname}",
        "format-disconnected": "NETWORK: DISCONNECTED",
        "tooltip-format": "{ifname} via {gwaddr}",
        "tooltip-format-wifi": "{essid} ({signalStrength}%)",
        "tooltip-format-ethernet": "{ifname}",
        "tooltip-format-disconnected": "Disconnected"
    },
    "bluetooth": {
        "format": "BLUETOOTH: {status}",
        "format-disabled": "BLUETOOTH: DISCONNECTED",
        "format-off": "BLUETOOTH: DISCONNECTED",
        "format-connected": "BLUETOOTH: {device_alias}",
        "format-connected-battery": "BLUETOOTH: {device_alias}",
        "tooltip-format": "{controller_alias}\t{controller_address}\n\n{num_connections} connected",
        "tooltip-format-connected": "{controller_alias}\t{controller_address}\n\n{num_connections} connected\n\n{device_enumerate}",
        "tooltip-format-enumerate-connected": "{device_alias}\t{device_address}",
        "tooltip-format-enumerate-connected-battery": "{device_alias}\t{device_address}\t{device_battery_percentage}%"
    },

    "backlight": {
        "format": "BRIGHTNESS: {percent} percent"
    },

    "clock": {
        "format": "{:%Y-%m-%d (%a) %H:%M:%S}",
        "interval": 1
    },

    "cpu": {
        "format": "CPU_USAGE: {usage} percent",
        "interval": 2
    },

    "memory": {
        "format": "MM_USAGE: {percentage} percent",
        "interval": 2
    },

    "disk": {
        "format": "STORAGE_USAGE: {percentage_used} percent",
        "path": "/"
    },

    "battery": {
        "format": "BATTERY: {status} {capacity} percent",
        "format-charging": "BATTERY: CHARGING {capacity} percent",
        "format-plugged": "BATTERY: CHARGED {capacity} percent",
        "format-full": "BATTERY: CHARGED {capacity} percent",
        "states": {
            "warning": 30,
            "critical": 15
        }
    }
}
